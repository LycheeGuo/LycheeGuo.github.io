---
title: Publications
cms_exclude: true

# 1. 视图选择 compact
view: compact

design:
  # 2. 必须设为 '1' (全宽模式)
  # 解释：虽然这里写的是全宽，但我已经在 view 模板里强制加了 "max-w-5xl" 限制
  # 所以它会自动变成你要的“中间窄条”样式，而不是全屏
  columns: '1' 
---
